---
description: Cancel an active background Codex job in this repository
---

Call the `codex_cancel` tool. The first non-flag token in the arguments is the `jobId`; omit it to cancel the only active job from this session.

Raw slash-command arguments:
`$ARGUMENTS`

Return the tool output verbatim.
